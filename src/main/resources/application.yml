spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/examportal
    username: postgres
    password: postgres

  jpa:

    hibernate:
      ddl-auto: update

    properties:

      hibernate:
        format_sql: true

#        dialect: org.hibernate.dialect.PostgreSQLDialect

    show-sql: true

